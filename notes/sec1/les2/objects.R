# Objects are "anything stored in R", such as these variables
# which are assigned using the assignment symbol <- (arrow)
a <- 1
b <- 1
c <- -1 

# Can print the variables by just writing their name, or print()
a 
print(a)

# Can use these variables in calculations, such as the quadratic formula
(-b + sqrt(b^2 - 4*a*c)) / (2*a)
(-b - sqrt(b^2 - 4*a*c)) / (2*a) 